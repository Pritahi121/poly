[build]
builder = "nixpacks"

[deploy]
startCommand = "./node_modules/.bin/prisma db push --accept-data-loss && node server.js"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
